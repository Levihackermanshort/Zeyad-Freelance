<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Zeyad Maeen — Freelance Services</title>
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=DM+Mono:wght@300;400;500&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #060810;
    --surface: #0d1117;
    --surface2: #141b26;
    --border: rgba(255,255,255,0.07);
    --accent: #00e5ff;
    --accent2: #7c3aed;
    --text: #f0f4ff;
    --muted: #7a8499;
  }
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
  html { scroll-behavior: smooth; }
  body { font-family: 'DM Mono', monospace; background: var(--bg); color: var(--text); overflow-x: hidden; cursor: none; }
 
  /* CURSOR */
  #cursor { position: fixed; width: 12px; height: 12px; background: var(--accent); border-radius: 50%; pointer-events: none; z-index: 99999; transform: translate(-50%,-50%); transition: width .2s, height .2s, opacity .2s; mix-blend-mode: screen; }
  #cursor-ring { position: fixed; width: 40px; height: 40px; border: 1px solid rgba(0,229,255,.5); border-radius: 50%; pointer-events: none; z-index: 99998; transform: translate(-50%,-50%); transition: width .4s cubic-bezier(.25,.46,.45,.94), height .4s cubic-bezier(.25,.46,.45,.94); }
  .cursor-big #cursor-ring { width: 60px; height: 60px; border-color: var(--accent); }
 
  /* CANVAS */
  #particle-canvas { position: fixed; inset: 0; z-index: 0; pointer-events: none; opacity: .6; }
 
  /* NOISE */
  body::before { content: ''; position: fixed; inset: 0; background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='.04'/%3E%3C/svg%3E"); pointer-events: none; z-index: 9998; opacity: .35; }
 
  /* SCROLL REVEAL */
  .reveal { opacity: 0; transform: translateY(40px); transition: opacity .8s cubic-bezier(.16,1,.3,1), transform .8s cubic-bezier(.16,1,.3,1); }
  .reveal.visible { opacity: 1; transform: none; }
  .reveal-left { opacity: 0; transform: translateX(-30px); transition: opacity .8s cubic-bezier(.16,1,.3,1), transform .8s cubic-bezier(.16,1,.3,1); }
  .reveal-left.visible { opacity: 1; transform: none; }
 
  /* NAV */
  nav { position: fixed; top: 0; left: 0; right: 0; z-index: 1000; display: flex; justify-content: space-between; align-items: center; padding: 1.2rem 8vw; transition: background .4s, backdrop-filter .4s, border-color .4s; border-bottom: 1px solid transparent; }
  nav.scrolled { background: rgba(6,8,16,.9); backdrop-filter: blur(20px); border-color: var(--border); }
  .nav-logo { font-family: 'Syne', sans-serif; font-weight: 800; font-size: 1.1rem; }
  .nav-logo span { color: var(--accent); }
  .nav-links { display: flex; gap: 2rem; list-style: none; }
  .nav-links a { text-decoration: none; color: var(--muted); font-size: .75rem; letter-spacing: .1em; text-transform: uppercase; transition: color .2s; position: relative; }
  .nav-links a::after { content: ''; position: absolute; bottom: -3px; left: 0; width: 0; height: 1px; background: var(--accent); transition: width .3s; }
  .nav-links a:hover { color: var(--accent); }
  .nav-links a:hover::after { width: 100%; }
 
  .btn-primary { display: inline-flex; align-items: center; gap: 10px; background: var(--accent); color: #060810; padding: 14px 28px; font-family: 'Syne', sans-serif; font-weight: 700; font-size: .85rem; letter-spacing: .05em; text-transform: uppercase; text-decoration: none; border-radius: 4px; transition: transform .2s, box-shadow .2s; position: relative; overflow: hidden; }
  .btn-primary::before { content: ''; position: absolute; inset: 0; background: linear-gradient(135deg, rgba(255,255,255,.2), transparent); transform: translateX(-100%); transition: transform .4s; }
  .btn-primary:hover::before { transform: translateX(100%); }
  .btn-primary:hover { transform: translateY(-2px); box-shadow: 0 8px 30px rgba(0,229,255,.4); }
 
  .btn-ghost { display: inline-flex; align-items: center; gap: 10px; background: transparent; color: var(--text); padding: 14px 28px; font-family: 'Syne', sans-serif; font-weight: 600; font-size: .85rem; letter-spacing: .05em; text-transform: uppercase; text-decoration: none; border: 1px solid var(--border); border-radius: 4px; transition: border-color .2s, color .2s, background .2s; }
  .btn-ghost:hover { border-color: rgba(0,229,255,.4); color: var(--accent); background: rgba(0,229,255,.05); }
 
  /* HERO */
  .hero { position: relative; min-height: 100vh; display: flex; flex-direction: column; justify-content: center; align-items: flex-start; padding: 8rem 8vw 4rem; z-index: 1; }
  .hero-orb { position: absolute; width: 700px; height: 700px; border-radius: 50%; background: radial-gradient(circle, rgba(0,229,255,.13) 0%, rgba(124,58,237,.09) 50%, transparent 70%); top: -150px; right: -150px; animation: orbFloat 8s ease-in-out infinite, orbMorph 12s ease-in-out infinite; pointer-events: none; filter: blur(1px); }
  .hero-orb2 { position: absolute; width: 500px; height: 500px; border-radius: 50%; background: radial-gradient(circle, rgba(124,58,237,.12) 0%, transparent 70%); bottom: -50px; left: 15%; animation: orbFloat2 10s ease-in-out infinite, orbMorph2 15s ease-in-out infinite; pointer-events: none; filter: blur(2px); }
  @keyframes orbFloat { 0%,100%{transform:translate(0,0) scale(1)} 33%{transform:translate(-20px,30px) scale(1.05)} 66%{transform:translate(20px,-15px) scale(.97)} }
  @keyframes orbFloat2 { 0%,100%{transform:translate(0,0) scale(1)} 33%{transform:translate(30px,-20px) scale(1.08)} 66%{transform:translate(-10px,25px) scale(.95)} }
  @keyframes orbMorph { 0%,100%{border-radius:50%} 25%{border-radius:60% 40% 55% 45%/50% 55% 45% 50%} 50%{border-radius:45% 55% 40% 60%/55% 45% 55% 45%} 75%{border-radius:55% 45% 60% 40%/45% 55% 45% 55%} }
  @keyframes orbMorph2 { 0%,100%{border-radius:50%} 33%{border-radius:40% 60% 45% 55%/55% 45% 60% 40%} 66%{border-radius:60% 40% 50% 50%/40% 60% 45% 55%} }
 
  .status-badge { display: inline-flex; align-items: center; gap: 8px; background: rgba(0,229,255,.08); border: 1px solid rgba(0,229,255,.2); border-radius: 100px; padding: 6px 16px; font-size: .7rem; letter-spacing: .15em; text-transform: uppercase; color: var(--accent); margin-bottom: 2rem; animation: fadeSlideUp .6s ease both; }
  .status-dot { width: 7px; height: 7px; border-radius: 50%; background: var(--accent); animation: blink 1.5s ease-in-out infinite; }
  @keyframes blink { 0%,100%{opacity:1;box-shadow:0 0 8px var(--accent),0 0 16px var(--accent)} 50%{opacity:.3;box-shadow:none} }
 
  .hero h1 { font-family: 'Syne', sans-serif; font-size: clamp(3rem,7vw,6.5rem); font-weight: 800; line-height: 1; letter-spacing: -.02em; animation: fadeSlideUp .7s .1s ease both; }
  .glitch-word { position: relative; display: inline-block; background: linear-gradient(135deg, var(--accent), var(--accent2)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }
  .glitch-word::before, .glitch-word::after { content: attr(data-text); position: absolute; left: 0; top: 0; background: linear-gradient(135deg, var(--accent), var(--accent2)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }
  .glitch-word::before { animation: glitch1 4s infinite steps(1); clip-path: polygon(0 0,100% 0,100% 35%,0 35%); }
  .glitch-word::after { animation: glitch2 4s infinite steps(1); clip-path: polygon(0 65%,100% 65%,100% 100%,0 100%); }
  @keyframes glitch1 { 0%,94%,100%{transform:none;opacity:0} 95%{transform:translate(-3px,1px);opacity:.8;filter:hue-rotate(90deg)} 97%{transform:translate(3px,-1px);opacity:.8} 99%{transform:translate(0,2px);opacity:0} }
  @keyframes glitch2 { 0%,94%,100%{transform:none;opacity:0} 96%{transform:translate(3px,-1px);opacity:.7;filter:hue-rotate(-60deg)} 98%{transform:translate(-2px,2px);opacity:.7} }
 
  .hero-sub { font-size: clamp(.9rem,1.5vw,1.05rem); color: var(--muted); margin-top: 1.5rem; max-width: 540px; line-height: 1.8; animation: fadeSlideUp .7s .2s ease both; }
  .typewriter { color: var(--accent); border-right: 2px solid var(--accent); animation: cursorBlink .7s step-end infinite; }
  @keyframes cursorBlink { 0%,100%{border-color:var(--accent)} 50%{border-color:transparent} }
 
  .hero-tags { display: flex; flex-wrap: wrap; gap: 10px; margin-top: 2rem; animation: fadeSlideUp .7s .3s ease both; }
  .tag { background: var(--surface2); border: 1px solid var(--border); padding: 5px 14px; border-radius: 4px; font-size: .7rem; letter-spacing: .08em; color: var(--muted); text-transform: uppercase; transition: border-color .2s, color .2s, transform .2s; }
  .tag:hover { border-color: rgba(0,229,255,.3); color: var(--accent); transform: translateY(-2px); }
  .hero-cta { margin-top: 3rem; display: flex; gap: 1rem; animation: fadeSlideUp .7s .4s ease both; }
 
  .scroll-indicator { position: absolute; bottom: 2.5rem; left: 8vw; display: flex; align-items: center; gap: 12px; font-size: .68rem; letter-spacing: .15em; text-transform: uppercase; color: var(--muted); animation: fadeSlideUp 1s .8s ease both; }
  .scroll-line { width: 40px; height: 1px; background: var(--muted); position: relative; overflow: hidden; }
  .scroll-line::after { content: ''; position: absolute; top: 0; left: -100%; width: 100%; height: 100%; background: var(--accent); animation: scrollLine 2s ease-in-out infinite; }
  @keyframes scrollLine { 0%{left:-100%} 100%{left:100%} }
  @keyframes fadeSlideUp { from{opacity:0;transform:translateY(24px)} to{opacity:1;transform:none} }
 
  /* MARQUEE */
  .marquee-wrap { position: relative; z-index: 1; overflow: hidden; border-top: 1px solid var(--border); border-bottom: 1px solid var(--border); padding: 1rem 0; background: var(--surface); }
  .marquee-track { display: flex; gap: 3rem; width: max-content; animation: marquee 20s linear infinite; }
  .marquee-item { display: flex; align-items: center; gap: 1rem; font-family: 'Syne', sans-serif; font-size: .75rem; font-weight: 700; letter-spacing: .15em; text-transform: uppercase; color: var(--muted); white-space: nowrap; }
  .marquee-item em { color: var(--accent); font-style: normal; font-size: 1rem; }
  @keyframes marquee { from{transform:translateX(0)} to{transform:translateX(-50%)} }
 
  /* STATS */
  .stats-bar { position: relative; z-index: 1; display: grid; grid-template-columns: repeat(auto-fit, minmax(160px,1fr)); gap: 1px; background: var(--border); border-top: 1px solid var(--border); border-bottom: 1px solid var(--border); }
  .stat { background: var(--bg); padding: 2.5rem 3rem; display: flex; flex-direction: column; gap: 6px; position: relative; overflow: hidden; transition: background .3s; }
  .stat:hover { background: var(--surface); }
  .stat::after { content: ''; position: absolute; bottom: 0; left: 0; width: 0; height: 2px; background: var(--accent); transition: width .4s; }
  .stat:hover::after { width: 100%; }
  .stat-num { font-family: 'Syne', sans-serif; font-size: 2.4rem; font-weight: 800; color: var(--accent); letter-spacing: -.02em; line-height: 1; }
  .stat-label { font-size: .68rem; color: var(--muted); text-transform: uppercase; letter-spacing: .12em; }
 
  /* SECTION */
  section { position: relative; z-index: 1; padding: 6rem 8vw; }
  .section-label { display: flex; align-items: center; gap: 12px; font-size: .7rem; letter-spacing: .2em; text-transform: uppercase; color: var(--accent); margin-bottom: 1rem; }
  .section-label::before { content: ''; width: 32px; height: 1px; background: var(--accent); }
  .section-title { font-family: 'Syne', sans-serif; font-size: clamp(2rem,4vw,3.2rem); font-weight: 800; line-height: 1.1; letter-spacing: -.02em; margin-bottom: 1rem; }
  .section-desc { color: var(--muted); font-size: .9rem; line-height: 1.8; max-width: 500px; margin-bottom: 3rem; }
 
  /* SERVICES */
  .services-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px,1fr)); gap: 1px; background: var(--border); border: 1px solid var(--border); border-radius: 8px; overflow: hidden; }
  .service-card { background: var(--surface); padding: 2.5rem; display: flex; flex-direction: column; gap: 1rem; transition: background .3s; position: relative; overflow: hidden; }
  .service-card::before { content: ''; position: absolute; top: 0; left: 0; right: 0; height: 2px; background: linear-gradient(90deg, transparent, var(--accent-card, var(--accent)), transparent); transform: scaleX(0); transition: transform .4s; }
  .service-card::after { content: ''; position: absolute; inset: 0; background: radial-gradient(circle at var(--mx,50%) var(--my,50%), rgba(0,229,255,.05) 0%, transparent 60%); opacity: 0; transition: opacity .3s; pointer-events: none; }
  .service-card:hover::before { transform: scaleX(1); }
  .service-card:hover::after { opacity: 1; }
  .service-card:hover { background: var(--surface2); }
  .service-icon { width: 48px; height: 48px; border-radius: 8px; display: flex; align-items: center; justify-content: center; font-size: 1.4rem; background: var(--icon-bg, rgba(0,229,255,.1)); border: 1px solid var(--icon-border, rgba(0,229,255,.2)); transition: transform .3s, box-shadow .3s; }
  .service-card:hover .service-icon { transform: scale(1.1) rotate(-5deg); box-shadow: 0 4px 20px var(--icon-bg); }
  .service-name { font-family: 'Syne', sans-serif; font-size: 1.05rem; font-weight: 700; }
  .service-desc { font-size: .8rem; color: var(--muted); line-height: 1.7; flex: 1; }
  .service-tags { display: flex; flex-wrap: wrap; gap: 6px; }
  .stag { font-size: .62rem; padding: 3px 10px; border-radius: 100px; border: 1px solid var(--border); color: var(--muted); transition: border-color .2s, color .2s; }
  .service-card:hover .stag { border-color: rgba(0,229,255,.2); color: var(--text); }
 
  /* PACKAGES */
  .packages-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px,1fr)); gap: 1.5rem; }
  .package-card { background: var(--surface); border: 1px solid var(--border); border-radius: 8px; padding: 2.5rem; display: flex; flex-direction: column; gap: 1.5rem; position: relative; transition: box-shadow .3s, border-color .3s; overflow: hidden; transform-style: preserve-3d; will-change: transform; }
  .package-card::before { content: ''; position: absolute; inset: 0; background: radial-gradient(ellipse at top, rgba(0,229,255,.04), transparent 60%); opacity: 0; transition: opacity .4s; }
  .package-card:hover::before { opacity: 1; }
  .package-card:hover { box-shadow: 0 20px 60px rgba(0,0,0,.4), 0 0 40px rgba(0,229,255,.08); }
  .package-card.featured { border-color: var(--accent); background: linear-gradient(135deg, rgba(0,229,255,.06), var(--surface)); }
  .package-card.featured::before { background: radial-gradient(ellipse at top, rgba(0,229,255,.1), transparent 60%); opacity: 1; }
  .featured-badge { position: absolute; top: -12px; left: 50%; transform: translateX(-50%); background: var(--accent); color: #060810; font-family: 'Syne', sans-serif; font-weight: 700; font-size: .65rem; letter-spacing: .1em; text-transform: uppercase; padding: 4px 16px; border-radius: 100px; white-space: nowrap; animation: badgePop .5s .3s cubic-bezier(.34,1.56,.64,1) both; }
  @keyframes badgePop { from{transform:translateX(-50%) scale(0)} to{transform:translateX(-50%) scale(1)} }
  .pkg-tier { font-size: .7rem; letter-spacing: .15em; text-transform: uppercase; color: var(--accent); }
  .pkg-name { font-family: 'Syne', sans-serif; font-size: 1.5rem; font-weight: 800; }
  .pkg-price { font-family: 'Syne', sans-serif; font-size: 2.8rem; font-weight: 800; letter-spacing: -.03em; line-height: 1; }
  .pkg-price span { font-size: 1rem; color: var(--muted); font-weight: 400; }
  .pkg-desc { font-size: .8rem; color: var(--muted); line-height: 1.7; border-top: 1px solid var(--border); padding-top: 1.5rem; }
  .pkg-features { list-style: none; display: flex; flex-direction: column; gap: 10px; }
  .pkg-features li { display: flex; align-items: flex-start; gap: 10px; font-size: .8rem; color: var(--muted); line-height: 1.5; opacity: 0; transform: translateX(-10px); transition: opacity .4s, transform .4s, color .2s; }
  .pkg-features li.shown { opacity: 1; transform: none; }
  .pkg-features li::before { content: '→'; color: var(--accent); flex-shrink: 0; margin-top: 1px; }
  .package-card:hover .pkg-features li { color: var(--text); }
  .pkg-btn { display: block; text-align: center; text-decoration: none; padding: 12px; border-radius: 4px; font-family: 'Syne', sans-serif; font-weight: 700; font-size: .8rem; letter-spacing: .08em; text-transform: uppercase; margin-top: auto; border: 1px solid var(--border); color: var(--text); transition: border-color .3s, color .3s; position: relative; overflow: hidden; }
  .pkg-btn::before { content: ''; position: absolute; top: 0; left: -100%; width: 100%; height: 100%; background: var(--accent); transition: left .3s; z-index: -1; }
  .pkg-btn:hover { border-color: var(--accent); color: #060810; }
  .pkg-btn:hover::before { left: 0; }
  .package-card.featured .pkg-btn { background: var(--accent); border-color: var(--accent); color: #060810; }
 
  /* PROCESS */
  .process-list { display: flex; flex-direction: column; gap: 1px; background: var(--border); border: 1px solid var(--border); border-radius: 8px; overflow: hidden; }
  .process-step { background: var(--surface); display: grid; grid-template-columns: 80px 1fr; gap: 2rem; align-items: start; padding: 2rem 2.5rem; transition: background .3s; position: relative; overflow: hidden; }
  .process-step::after { content: ''; position: absolute; left: 0; top: 0; bottom: 0; width: 0; background: linear-gradient(90deg, rgba(0,229,255,.04), transparent); transition: width .4s; }
  .process-step:hover { background: var(--surface2); }
  .process-step:hover::after { width: 100%; }
  .step-num { font-family: 'Syne', sans-serif; font-size: 2.5rem; font-weight: 800; color: rgba(0,229,255,.12); line-height: 1; letter-spacing: -.04em; transition: color .3s; }
  .process-step:hover .step-num { color: rgba(0,229,255,.3); }
  .step-content h3 { font-family: 'Syne', sans-serif; font-size: 1rem; font-weight: 700; margin-bottom: 6px; }
  .step-content p { font-size: .8rem; color: var(--muted); line-height: 1.7; }
 
  /* TECH */
  .tech-grid { display: flex; flex-wrap: wrap; gap: 10px; }
  .tech-pill { background: var(--surface2); border: 1px solid var(--border); padding: 8px 18px; border-radius: 100px; font-size: .75rem; display: flex; align-items: center; gap: 8px; transition: border-color .2s, color .2s, transform .2s, box-shadow .2s; cursor: default; }
  .tech-pill:hover { border-color: rgba(0,229,255,.4); color: var(--accent); transform: translateY(-3px); box-shadow: 0 6px 20px rgba(0,229,255,.1); }
  .tech-dot { width: 6px; height: 6px; border-radius: 50%; background: var(--accent); opacity: .5; transition: opacity .2s, box-shadow .2s; }
  .tech-pill:hover .tech-dot { opacity: 1; box-shadow: 0 0 6px var(--accent); }
 
  /* CTA */
  .cta-section { position: relative; z-index: 1; background: var(--surface); border-top: 1px solid var(--border); border-bottom: 1px solid var(--border); padding: 6rem 8vw; display: grid; grid-template-columns: 1fr auto; gap: 2rem; align-items: center; overflow: hidden; }
  .cta-section::before { content: ''; position: absolute; width: 600px; height: 600px; background: radial-gradient(circle, rgba(0,229,255,.07) 0%, transparent 70%); top: 50%; left: -100px; transform: translateY(-50%); animation: ctaOrb 5s ease-in-out infinite; pointer-events: none; }
  @keyframes ctaOrb { 0%,100%{transform:translateY(-50%) scale(1)} 50%{transform:translateY(-50%) scale(1.1)} }
  .cta-section h2 { font-family: 'Syne', sans-serif; font-size: clamp(1.8rem,3.5vw,3rem); font-weight: 800; line-height: 1.1; position: relative; }
  .cta-section h2 span { color: var(--accent); }
  .cta-sub { color: var(--muted); font-size: .85rem; margin-top: .8rem; line-height: 1.7; }
 
  footer { position: relative; z-index: 1; padding: 2rem 8vw; display: flex; justify-content: space-between; align-items: center; border-top: 1px solid var(--border); font-size: .72rem; color: var(--muted); }
  footer a { color: var(--accent); text-decoration: none; }
  .divider { height: 1px; background: linear-gradient(90deg, transparent, var(--border), transparent); margin: 0 8vw; position: relative; z-index: 1; }
 
  @media (max-width: 768px) {
    .nav-links { display: none; }
    .cta-section { grid-template-columns: 1fr; }
    .hero, section { padding: 7rem 6vw 4rem; }
    #cursor, #cursor-ring { display: none; }
    body { cursor: auto; }
  }
</style>
</head>
<body>
 
<div id="cursor"></div>
<div id="cursor-ring"></div>
<canvas id="particle-canvas"></canvas>
 
<nav id="nav">
  <div class="nav-logo">ZM<span>.</span></div>
  <ul class="nav-links">
    <li><a href="#services">Services</a></li>
    <li><a href="#packages">Packages</a></li>
    <li><a href="#process">Process</a></li>
    <li><a href="#contact">Contact</a></li>
  </ul>
  <a href="mailto:zeyadmaeen1@gmail.com" class="btn-primary" style="padding:10px 20px;font-size:.72rem;">Hire Me →</a>
</nav>
 
<section class="hero">
  <div class="hero-orb"></div>
  <div class="hero-orb2"></div>
  <div class="status-badge"><span class="status-dot"></span>Available for Projects</div>
  <h1>Full-Stack<br>Dev & <span class="glitch-word" data-text="AI Engineer">AI Engineer</span></h1>
  <p class="hero-sub">I'm Zeyad Maeen — software engineer, backend developer, and AI researcher with 5+ years of experience. Building <span class="typewriter" id="typewriter"></span></p>
  <div class="hero-tags">
    <span class="tag">Backend Dev</span><span class="tag">AI & ML</span><span class="tag">Web Apps</span><span class="tag">UI Design</span><span class="tag">Cloud (AWS)</span><span class="tag">Data Analysis</span><span class="tag">Database Mgmt</span>
  </div>
  <div class="hero-cta">
    <a href="#packages" class="btn-primary">View Packages →</a>
    <a href="https://levihackermanshort.github.io/Portfolio/" target="_blank" class="btn-ghost">Portfolio ↗</a>
  </div>
  <div class="scroll-indicator"><div class="scroll-line"></div>Scroll to explore</div>
</section>
 
<div class="marquee-wrap">
  <div class="marquee-track">
    <div class="marquee-item"><em>✦</em>Backend Development</div><div class="marquee-item"><em>✦</em>AI Integration</div><div class="marquee-item"><em>✦</em>React Applications</div><div class="marquee-item"><em>✦</em>Database Architecture</div><div class="marquee-item"><em>✦</em>UI/UX Design</div><div class="marquee-item"><em>✦</em>AWS Cloud</div><div class="marquee-item"><em>✦</em>Data Analysis</div><div class="marquee-item"><em>✦</em>Node.js</div><div class="marquee-item"><em>✦</em>Python</div><div class="marquee-item"><em>✦</em>Deep Learning</div>
    <div class="marquee-item"><em>✦</em>Backend Development</div><div class="marquee-item"><em>✦</em>AI Integration</div><div class="marquee-item"><em>✦</em>React Applications</div><div class="marquee-item"><em>✦</em>Database Architecture</div><div class="marquee-item"><em>✦</em>UI/UX Design</div><div class="marquee-item"><em>✦</em>AWS Cloud</div><div class="marquee-item"><em>✦</em>Data Analysis</div><div class="marquee-item"><em>✦</em>Node.js</div><div class="marquee-item"><em>✦</em>Python</div><div class="marquee-item"><em>✦</em>Deep Learning</div>
  </div>
</div>
 
<div class="stats-bar">
  <div class="stat reveal"><div class="stat-num"><span class="count-up" data-target="5">0</span>+</div><div class="stat-label">Years Experience</div></div>
  <div class="stat reveal" style="transition-delay:.1s"><div class="stat-num"><span class="count-up" data-target="10">0</span>+</div><div class="stat-label">Projects Shipped</div></div>
  <div class="stat reveal" style="transition-delay:.2s"><div class="stat-num"><span class="count-up" data-target="4">0</span></div><div class="stat-label">Languages Spoken</div></div>
  <div class="stat reveal" style="transition-delay:.3s"><div class="stat-num"><span class="count-up" data-target="3">0</span>+</div><div class="stat-label">Internships & Roles</div></div>
  <div class="stat reveal" style="transition-delay:.4s"><div class="stat-num">∞</div><div class="stat-label">Revisions on Pro+</div></div>
</div>
 
<section id="services">
  <div class="section-label reveal-left">What I Do</div>
  <h2 class="section-title reveal">Services</h2>
  <p class="section-desc reveal">End-to-end digital services, from production-grade web apps to AI integrations, UI design, and data pipelines.</p>
  <div class="services-grid">
    <div class="service-card reveal" style="--accent-card:#00e5ff;--icon-bg:rgba(0,229,255,.1);--icon-border:rgba(0,229,255,.2)"><div class="service-icon">⚙️</div><div class="service-name">Backend Development</div><p class="service-desc">Robust APIs, server logic, and database architecture. Experienced with Node.js, Django, Flask, PostgreSQL, MySQL, and Firebase. Built production systems at Radical AI.</p><div class="service-tags"><span class="stag">Node.js</span><span class="stag">Django</span><span class="stag">Flask</span><span class="stag">PostgreSQL</span><span class="stag">Firebase</span></div></div>
    <div class="service-card reveal" style="--accent-card:#7c3aed;--icon-bg:rgba(124,58,237,.1);--icon-border:rgba(124,58,237,.2);transition-delay:.05s"><div class="service-icon">🤖</div><div class="service-name">AI Integration & Research</div><p class="service-desc">Integrate LLMs, build AI-powered features, or research custom solutions. Active creator at Rosebud AI (Enigma AI) and experienced with deep learning and AI pipelines.</p><div class="service-tags"><span class="stag">LLMs</span><span class="stag">Deep Learning</span><span class="stag">Python</span><span class="stag">Research</span></div></div>
    <div class="service-card reveal" style="--accent-card:#f59e0b;--icon-bg:rgba(245,158,11,.1);--icon-border:rgba(245,158,11,.2);transition-delay:.1s"><div class="service-icon">🌐</div><div class="service-name">Full-Stack Web Apps</div><p class="service-desc">From landing pages to complete React applications. Frontend built with React.js, backends powered by Node.js or Django, deployed to cloud infrastructure.</p><div class="service-tags"><span class="stag">React.js</span><span class="stag">HTML/CSS</span><span class="stag">JavaScript</span><span class="stag">Firebase</span></div></div>
    <div class="service-card reveal" style="--accent-card:#10b981;--icon-bg:rgba(16,185,129,.1);--icon-border:rgba(16,185,129,.2);transition-delay:.15s"><div class="service-icon">🗄️</div><div class="service-name">Database Design & Optimization</div><p class="service-desc">Schema design, query optimization, and database management. Trained in Oracle Java JDBC and TOAD. Proven record improving query efficiency by 15%+ through targeted optimization.</p><div class="service-tags"><span class="stag">MySQL</span><span class="stag">PostgreSQL</span><span class="stag">Oracle</span><span class="stag">TOAD</span></div></div>
    <div class="service-card reveal" style="--accent-card:#e879f9;--icon-bg:rgba(232,121,249,.1);--icon-border:rgba(232,121,249,.2);transition-delay:.2s"><div class="service-icon">🎨</div><div class="service-name">UI/UX Design</div><p class="service-desc">User-centered design using Google UX Design methodologies. Certified UI Design Specialist from University of Minnesota. Clean interfaces backed by real design principles.</p><div class="service-tags"><span class="stag">Figma</span><span class="stag">UX Research</span><span class="stag">Prototyping</span><span class="stag">Design Systems</span></div></div>
    <div class="service-card reveal" style="--accent-card:#f97316;--icon-bg:rgba(249,115,22,.1);--icon-border:rgba(249,115,22,.2);transition-delay:.25s"><div class="service-icon">☁️</div><div class="service-name">Cloud & Data Analysis</div><p class="service-desc">AWS cloud computing, data pipelines, and analytical reporting. Training in AWS Cloud (Manara) and Data Analysis (Alx Ventures). Perfect for data-driven product decisions.</p><div class="service-tags"><span class="stag">AWS</span><span class="stag">Data Analysis</span><span class="stag">Cloud Infra</span><span class="stag">Python</span></div></div>
  </div>
</section>
 
<div class="divider"></div>
 
<section id="packages">
  <div class="section-label reveal-left">Pricing</div>
  <h2 class="section-title reveal">Packages</h2>
  <p class="section-desc reveal">Transparent, flat-rate packages. Need something custom? Let's talk.</p>
  <div class="packages-grid">
    <div class="package-card reveal">
      <div><div class="pkg-tier">Starter</div><div class="pkg-name">Launch</div></div>
      <div class="pkg-price">$299<span> /project</span></div>
      <p class="pkg-desc">Perfect for individuals and small businesses who need a clean, professional online presence built fast.</p>
      <ul class="pkg-features"><li>Single-page website or landing page</li><li>Responsive HTML/CSS/JS or React</li><li>Contact form with email integration</li><li>Basic SEO setup</li><li>2 revision rounds</li><li>5-day delivery</li><li>Firebase or static hosting setup</li></ul>
      <a href="mailto:zeyadmaeen1@gmail.com?subject=Launch Package" class="pkg-btn">Get Started →</a>
    </div>
    <div class="package-card featured reveal" style="transition-delay:.1s">
      <div class="featured-badge">Most Popular</div>
      <div><div class="pkg-tier">Growth</div><div class="pkg-name">Build</div></div>
      <div class="pkg-price">$799<span> /project</span></div>
      <p class="pkg-desc">For startups and SMEs who need a full-featured web application with backend, database, and clean UX.</p>
      <ul class="pkg-features"><li>Multi-page React web application</li><li>Backend API (Node.js or Django)</li><li>Database design & setup (PostgreSQL / MySQL)</li><li>User auth (login, register, roles)</li><li>Admin dashboard or CMS</li><li>4 revision rounds</li><li>Cloud deployment (Firebase / AWS)</li><li>2 weeks delivery</li><li>30-day post-launch support</li></ul>
      <a href="mailto:zeyadmaeen1@gmail.com?subject=Build Package" class="pkg-btn">Get Started →</a>
    </div>
    <div class="package-card reveal" style="transition-delay:.2s">
      <div><div class="pkg-tier">Advanced</div><div class="pkg-name">AI-Powered</div></div>
      <div class="pkg-price">$1,499<span> /project</span></div>
      <p class="pkg-desc">For clients who want to leverage AI — chatbots, intelligent automation, data pipelines, or LLM integrations.</p>
      <ul class="pkg-features"><li>Everything in Build package</li><li>AI/LLM integration (OpenAI, custom models)</li><li>Data pipeline or analytics dashboard</li><li>Deep learning / recommendation system</li><li>AWS cloud architecture & deployment</li><li>Unlimited revisions</li><li>3 weeks delivery</li><li>60-day post-launch support</li><li>Technical documentation included</li></ul>
      <a href="mailto:zeyadmaeen1@gmail.com?subject=AI-Powered Package" class="pkg-btn">Get Started →</a>
    </div>
    <div class="package-card reveal" style="grid-column:1/-1;display:grid;grid-template-columns:1fr 1fr;gap:2rem;transition-delay:.3s">
      <div style="display:flex;flex-direction:column;gap:1rem">
        <div class="pkg-tier">Retainer</div><div class="pkg-name">Monthly Partner</div>
        <div class="pkg-price">$599<span> /month</span></div>
        <p class="pkg-desc">Ongoing technical support, feature development, and consulting. Ideal for businesses needing a dedicated developer without hiring full-time. Min. 2-month commitment.</p>
      </div>
      <div style="display:flex;flex-direction:column;justify-content:space-between">
        <ul class="pkg-features"><li>40 hours of development / month</li><li>Bug fixes & maintenance priority</li><li>New feature development</li><li>Weekly progress calls</li><li>Database & performance monitoring</li><li>AI/data consulting included</li><li>Rollover up to 8 unused hours</li></ul>
        <a href="mailto:zeyadmaeen1@gmail.com?subject=Monthly Retainer" class="pkg-btn" style="margin-top:1.5rem">Start Partnership →</a>
      </div>
    </div>
  </div>
</section>
 
<div class="divider"></div>
 
<section>
  <div class="section-label reveal-left">Tech Stack</div>
  <h2 class="section-title reveal">Tools & Technologies</h2>
  <p class="section-desc reveal">The full toolkit — everything I use to build reliable, scalable products.</p>
  <div class="tech-grid reveal">
    <span class="tech-pill"><span class="tech-dot"></span>Python</span><span class="tech-pill"><span class="tech-dot"></span>JavaScript</span><span class="tech-pill"><span class="tech-dot"></span>C / C++</span><span class="tech-pill"><span class="tech-dot"></span>Java</span><span class="tech-pill"><span class="tech-dot"></span>React.js</span><span class="tech-pill"><span class="tech-dot"></span>Node.js</span><span class="tech-pill"><span class="tech-dot"></span>Django</span><span class="tech-pill"><span class="tech-dot"></span>Flask</span><span class="tech-pill"><span class="tech-dot"></span>HTML / CSS</span><span class="tech-pill"><span class="tech-dot"></span>Firebase</span><span class="tech-pill"><span class="tech-dot"></span>PostgreSQL</span><span class="tech-pill"><span class="tech-dot"></span>MySQL</span><span class="tech-pill"><span class="tech-dot"></span>Oracle SQL</span><span class="tech-pill"><span class="tech-dot"></span>AWS Cloud</span><span class="tech-pill"><span class="tech-dot"></span>Three.js / WebGL</span><span class="tech-pill"><span class="tech-dot"></span>Git / GitHub</span><span class="tech-pill"><span class="tech-dot"></span>Figma</span><span class="tech-pill"><span class="tech-dot"></span>LLM / AI APIs</span><span class="tech-pill"><span class="tech-dot"></span>Deep Learning (GPU)</span><span class="tech-pill"><span class="tech-dot"></span>Data Analysis</span>
  </div>
</section>
 
<div class="divider"></div>
 
<section id="process">
  <div class="section-label reveal-left">How It Works</div>
  <h2 class="section-title reveal">The Process</h2>
  <p class="section-desc reveal">Clear, professional, no surprises. Here's exactly what working with me looks like.</p>
  <div class="process-list">
    <div class="process-step reveal"><div class="step-num">01</div><div class="step-content"><h3>Discovery Call</h3><p>We jump on a free 30-min call to understand your goals, timeline, and technical requirements. I'll ask the right questions so we're aligned before any code is written.</p></div></div>
    <div class="process-step reveal" style="transition-delay:.1s"><div class="step-num">02</div><div class="step-content"><h3>Proposal & Scope</h3><p>I send a clear written proposal outlining deliverables, milestones, timeline, and pricing. No scope creep — everything is defined upfront.</p></div></div>
    <div class="process-step reveal" style="transition-delay:.15s"><div class="step-num">03</div><div class="step-content"><h3>Build & Iterate</h3><p>Development begins with regular updates. You'll see progress at key milestones and have opportunities to give feedback throughout the build — not just at the end.</p></div></div>
    <div class="process-step reveal" style="transition-delay:.2s"><div class="step-num">04</div><div class="step-content"><h3>Review & QA</h3><p>Comprehensive testing across devices and environments. I handle app testing and quality assurance before anything goes live — no rushed deployments.</p></div></div>
    <div class="process-step reveal" style="transition-delay:.25s"><div class="step-num">05</div><div class="step-content"><h3>Delivery & Support</h3><p>You receive the final product, full source code, and documentation. Post-launch support is included in all packages — you won't be left alone after handoff.</p></div></div>
  </div>
</section>
 
<div class="cta-section" id="contact">
  <div class="reveal">
    <h2>Ready to build<br>something <span>great?</span></h2>
    <p class="cta-sub">Send me an email or connect on LinkedIn. I respond within 24 hours.<br>English 🇬🇧 · Arabic 🇪🇬 · German 🇩🇪</p>
  </div>
  <div style="display:flex;flex-direction:column;gap:12px;flex-shrink:0" class="reveal" style="transition-delay:.2s">
    <a href="mailto:zeyadmaeen1@gmail.com" class="btn-primary">Email Me →</a>
    <a href="https://linkedin.com/in/zezomaeen" target="_blank" class="btn-ghost">LinkedIn ↗</a>
  </div>
</div>
 
<footer>
  <div>© 2025 Zeyad Maeen — Freelance Software Engineer</div>
  <div><a href="https://levihackermanshort.github.io/Portfolio/" target="_blank">Portfolio ↗</a> &nbsp;·&nbsp; <a href="mailto:zeyadmaeen1@gmail.com">zeyadmaeen1@gmail.com</a></div>
</footer>
 
<script>
/* ── PARTICLE CANVAS ─────────────────────────────────── */
const canvas = document.getElementById('particle-canvas');
const ctx = canvas.getContext('2d');
let W, H, mouse = {x:-9999,y:-9999};
const particles = [];
 
function resize(){ W = canvas.width = window.innerWidth; H = canvas.height = window.innerHeight; }
resize();
window.addEventListener('resize', resize);
 
class Particle {
  constructor(){ this.reset(true); }
  reset(init){
    this.x = Math.random()*W; this.y = init ? Math.random()*H : (Math.random()>.5 ? -5 : H+5);
    this.size = Math.random()*1.5+.3;
    this.vx = (Math.random()-.5)*.4; this.vy = (Math.random()-.5)*.4;
    this.opacity = Math.random()*.5+.1;
    this.hue = Math.random()>.65 ? 280 : 190;
  }
  update(){
    this.x+=this.vx; this.y+=this.vy;
    const dx=this.x-mouse.x, dy=this.y-mouse.y, d=Math.hypot(dx,dy);
    if(d<120){ const f=(120-d)/120; this.x+=dx/d*f*2; this.y+=dy/d*f*2; }
    if(this.x<-10||this.x>W+10||this.y<-10||this.y>H+10) this.reset(false);
  }
  draw(){
    ctx.beginPath(); ctx.arc(this.x,this.y,this.size,0,Math.PI*2);
    ctx.fillStyle=`hsla(${this.hue},100%,70%,${this.opacity})`; ctx.fill();
  }
}
for(let i=0;i<120;i++) particles.push(new Particle());
 
function drawLines(){
  for(let i=0;i<particles.length;i++)
    for(let j=i+1;j<particles.length;j++){
      const dx=particles[i].x-particles[j].x, dy=particles[i].y-particles[j].y, d=Math.hypot(dx,dy);
      if(d<80){ ctx.beginPath(); ctx.moveTo(particles[i].x,particles[i].y); ctx.lineTo(particles[j].x,particles[j].y); ctx.strokeStyle=`rgba(0,229,255,${.06*(1-d/80)})`; ctx.lineWidth=.5; ctx.stroke(); }
    }
}
function tick(){ ctx.clearRect(0,0,W,H); drawLines(); particles.forEach(p=>{p.update();p.draw();}); requestAnimationFrame(tick); }
tick();
 
window.addEventListener('mousemove', e=>{mouse.x=e.clientX; mouse.y=e.clientY;});
 
/* ── CUSTOM CURSOR ──────────────────────────────────── */
const cur=document.getElementById('cursor'), ring=document.getElementById('cursor-ring');
let cx=0,cy=0,rx=0,ry=0;
window.addEventListener('mousemove',e=>{cx=e.clientX;cy=e.clientY;});
(function animCursor(){ cur.style.left=cx+'px'; cur.style.top=cy+'px'; rx+=(cx-rx)*.12; ry+=(cy-ry)*.12; ring.style.left=rx+'px'; ring.style.top=ry+'px'; requestAnimationFrame(animCursor); })();
 
document.querySelectorAll('a,button,.tech-pill,.tag,.service-card,.package-card').forEach(el=>{
  el.addEventListener('mouseenter',()=>{cur.style.width='6px';cur.style.height='6px';cur.style.opacity='.5';document.body.classList.add('cursor-big');});
  el.addEventListener('mouseleave',()=>{cur.style.width='12px';cur.style.height='12px';cur.style.opacity='1';document.body.classList.remove('cursor-big');});
});
 
/* ── SERVICE CARD LIGHT FOLLOW ───────────────────────── */
document.querySelectorAll('.service-card').forEach(card=>{
  card.addEventListener('mousemove',e=>{
    const r=card.getBoundingClientRect();
    card.style.setProperty('--mx',((e.clientX-r.left)/r.width*100).toFixed(1)+'%');
    card.style.setProperty('--my',((e.clientY-r.top)/r.height*100).toFixed(1)+'%');
  });
});
 
/* ── NAV SCROLL ──────────────────────────────────────── */
window.addEventListener('scroll',()=>document.getElementById('nav').classList.toggle('scrolled',scrollY>50));
 
/* ── TYPEWRITER ──────────────────────────────────────── */
const phrases=['clean, scalable web apps.','AI-powered products.','beautiful user experiences.','robust backend systems.','data-driven dashboards.'];
let pi=0,ci=0,del=false;
const tw=document.getElementById('typewriter');
function type(){
  const p=phrases[pi];
  if(!del){ tw.textContent=p.slice(0,++ci); if(ci===p.length){del=true;setTimeout(type,1800);return;} }
  else { tw.textContent=p.slice(0,--ci); if(ci===0){del=false;pi=(pi+1)%phrases.length;setTimeout(type,400);return;} }
  setTimeout(type,del?40:65);
}
type();
 
/* ── SCROLL REVEAL ───────────────────────────────────── */
new IntersectionObserver((entries)=>{
  entries.forEach(e=>{
    if(e.isIntersecting){
      e.target.classList.add('visible');
      e.target.querySelectorAll('.pkg-features li').forEach((li,i)=>setTimeout(()=>li.classList.add('shown'),i*80));
    }
  });
},{threshold:.1,rootMargin:'0px 0px -40px 0px'}).observe.bind(
  new IntersectionObserver((entries)=>{entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');e.target.querySelectorAll('.pkg-features li').forEach((li,i)=>setTimeout(()=>li.classList.add('shown'),i*80));}});},{threshold:.1,rootMargin:'0px 0px -40px 0px'})
);
 
// cleaner version:
const revObs = new IntersectionObserver(entries=>{
  entries.forEach(e=>{
    if(!e.isIntersecting) return;
    e.target.classList.add('visible');
    e.target.querySelectorAll('.pkg-features li').forEach((li,i)=>setTimeout(()=>li.classList.add('shown'),i*80));
  });
},{threshold:.1,rootMargin:'0px 0px -40px 0px'});
document.querySelectorAll('.reveal,.reveal-left').forEach(el=>revObs.observe(el));
 
/* ── COUNT UP ────────────────────────────────────────── */
new IntersectionObserver(entries=>{
  entries.forEach(e=>{
    if(!e.isIntersecting) return;
    const el=e.target, target=+el.dataset.target; let cur=0;
    const t=setInterval(()=>{ cur=Math.min(cur+target/40,target); el.textContent=Math.floor(cur); if(cur>=target)clearInterval(t); },35);
    countObs.unobserve(el);
  });
},{threshold:.5});
 
const countObs=new IntersectionObserver(entries=>{
  entries.forEach(e=>{
    if(!e.isIntersecting) return;
    const el=e.target,target=+el.dataset.target; let n=0;
    const t=setInterval(()=>{ n=Math.min(n+target/40,target); el.textContent=Math.floor(n); if(n>=target)clearInterval(t); },35);
    countObs.unobserve(el);
  });
},{threshold:.5});
document.querySelectorAll('.count-up').forEach(el=>countObs.observe(el));
 
/* ── PACKAGE CARD 3D TILT ────────────────────────────── */
document.querySelectorAll('.package-card').forEach(card=>{
  card.addEventListener('mousemove',e=>{
    const r=card.getBoundingClientRect(), x=(e.clientX-r.left)/r.width-.5, y=(e.clientY-r.top)/r.height-.5;
    card.style.transform=`translateY(-6px) rotateX(${-y*6}deg) rotateY(${x*6}deg) scale(1.01)`;
    card.style.transition='box-shadow .3s,border-color .3s';
  });
  card.addEventListener('mouseleave',()=>{ card.style.transform=''; card.style.transition='transform .6s cubic-bezier(.16,1,.3,1),box-shadow .3s,border-color .3s'; });
});
 
/* ── TECH PILLS STAGGER ──────────────────────────────── */
new IntersectionObserver(entries=>{
  entries.forEach(e=>{
    if(!e.isIntersecting) return;
    e.target.querySelectorAll('.tech-pill').forEach((p,i)=>{
      p.style.opacity='0'; p.style.transform='translateY(15px)';
      setTimeout(()=>{ p.style.transition=`opacity .4s ease ${i*30}ms, transform .4s ease ${i*30}ms`; p.style.opacity='1'; p.style.transform='translateY(0)'; },50);
    });
  });
},{threshold:.2}).observe(document.querySelector('.tech-grid'));
</script>
</body>
</html>
